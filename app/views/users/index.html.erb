<div id="pageContentHeader">
	Listing user
</div>

<table>
  <tr>
    <th>Username</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |listuser| %>
  <tr>
    <td><%= listuser.username %></td>
    <td>*************<!--%= listuser.password %--></td>
    <td><%= link_to 'Show', listuser %></td>
    <td><%= link_to 'Edit', edit_user_path(listuser) %></td>
    <td><%= link_to 'Destroy', listuser, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
